#include <bits/stdc++.h>
using namespace std;

int main () {

    int a, b, c; cin >> a >> b >> c; 
    double mid =  (a * 2 + b * 2 + c) / 5.0; 
    cout << setprecision(1) << fixed << mid; 
    
    return 0; 
}
