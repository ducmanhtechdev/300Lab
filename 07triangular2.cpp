#include <bits/stdc++.h>
using namespace std;

int main () {

    int a, h; cin >> a >> h; 
// dien tich
    double s = a * h / 2.0; 
// in dien tich -- 2 so thap phan
    cout << setprecision(2) << fixed << s; 
    
    return 0; 
}
